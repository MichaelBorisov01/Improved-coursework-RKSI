# # Игнорировать временные файлы Visual Studio, результаты сборки и
# # файлы, созданные популярными надстройками Visual Studio.
# #
# # Получайте последние новости с https://github.com/github/gitignore/blob/master/VisualStudio.gitignore

# Пользовательские файлы
* .rsuser
* .suo
* .user
* .userosscache
* .sln.docstates

# Пользовательские файлы (MonoDevelop / Xamarin Studio)
* .userprefs

# Моно автоматически сгенерированные файлы
mono_crash. *

# Построить результаты
[ Dd ] ebug /
[ Dd ] ebugPublic /
[ Rr ] легкость /
[ Rr ] Eleases /
х64 /
х86 /
[ Аа ] [ Rr ] [ Мм ] /
[ Аа ] [ Rr ] [ Мм ] 64 /
дом /
[ Bb ] в /
[ OO ] BJ /
[ Ll ] ог /
[ Ll ] ogs /

# Visual Studio 2015/2017 каталог кэша / параметров
.vs /
# Раскомментируйте, если у вас есть задачи, которые создают статические файлы проекта в wwwroot
# wwwroot /

# Visual Studio 2017 автоматически сгенерированные файлы
Сгенерировано \ Files /

# Результаты теста MSTest
[ Tt ] est [ Rr ] esult * /
[ Bb ] uild [ Ll ] ог. *

# NUnit
* .VisualState.xml
TestResult.xml
nunit- * .xml

# Результаты сборки проекта ATL
[ Dd ] ebugPS /
[ Rr ] eleasePS /
dlldata.c

# Результаты теста
BenchmarkDotNet.Artifacts /

# .NET Core
project.lock.json
project.fragment.lock.json
артефакты /

# StyleCop
StyleCopReport.xml

# Файлы, созданные Visual Studio
* _i.c
* _p.c
* _h.h
* .ilk
* .meta
* .obj
* .iobj
* .pch
* .pdb
* .ipdb
* .pgc
* .pgd
* .rsp
* .sbr
* .tlb
* .tli
* .tlh
* .tmp
* .tmp_proj
* _wpftmp.csproj
* .log
* .vspscc
* .vssscc
.builds
* .pidb
* .svclog
* .scc

# Chutzpah Тестовые файлы
_Chutzpah *

# Кэш-файлы Visual C ++
ipch /
* .aps
* .ncb
* .opendb
* .opensdf
* .sdf
* .cachefile
* .VC.db
* .VC.VC.opendb

# Visual Studio профилировщик
* .psess
* .vsp
* .vspx
* .sap

# Файлы трассировки Visual Studio
* .e2e

# TFS 2012 Local Workspace
$ tf /

# Инструментарий автоматизации навигации
* .gpState

# ReSharper - надстройка для кодирования .NET
_ReSharper * /
* . [ Rr ] e [ Ss ] Харпер
* .DotSettings.user

# TeamCity - это надстройка для сборки
_TeamCity *

# DotCover - инструмент покрытия кода
* .dotCover

# AxoCover - инструмент покрытия кода
.axoCover / *
! .axoCover / settings.json

# Coverlet - бесплатный кроссплатформенный инструмент для охвата кода
покрытие * [ .json, .xml, .info ]

# Результаты покрытия кода Visual Studio
*. покрытие
* .coveragexml

# NCrunch
_NCrunch_ *
, * crunch * .local.xml
nCrunchTemp_ *

# MightyMoose
* ммм *
АвтоТест.Нет /

# Веб-верстак (sass)
.sass-cache /

# Installshield выходная папка
[ Ee ] xpress /

# DocProject является надстройкой генератора документации
DocProject / buildhelp /
DocProject / Помощь / * .HxT
DocProject / Help / * .HxC
DocProject / Help / * .hhc
DocProject / Help / * .hhk
DocProject / Help / * .hhp
DocProject / Help / Html2
DocProject / Help / html

# Click-Once каталог
опубликовать /

# Опубликовать веб-вывод
* . [ Pp ] ublish.xml
* .azurePubxml
# Примечание. Прокомментируйте следующую строку, если хотите проверить настройки веб-развертывания,
# но строки подключения к базе данных (с потенциальными паролями) будут незашифрованными
* .pubxml
* .publishproj

# Настройки публикации Microsoft Azure Web App. Прокомментируйте следующую строку, если хотите
# проверьте настройки публикации Azure Web App, но содержалась конфиденциальная информация
# в этих скриптах будут незашифрованы
PublishScripts /

# NuGet Packages
* .nupkg
# NuGet Symbol Packages
* .snupkg
# Папка пакетов может быть проигнорирована из-за восстановления пакета
** / [ Pp ] ackages / *
# кроме build /, который используется в качестве цели MSBuild.
! ** / [ Pp ] ackages / build /
# Раскомментируйте при необходимости, однако, как правило, он будет восстановлен при необходимости
# ! ** / [Pp] ackages / repositories.config
# Файлы project.json в NuGet v3 создают больше игнорируемых файлов
* .nuget.props
* .nuget.targets

# Microsoft Azure Build Output
CSX /
* .build.csdef

# Эмулятор Microsoft Azure
ECF /
рсф /

# Каталоги и файлы пакетов приложений Магазина Windows
AppPackages /
BundleArtifacts /
Package.StoreAssociation.xml
_pkginfo.txt
* .appx
* .appxbundle
* .appxupload

# Файлы кеша Visual Studio
# файлы, оканчивающиеся на .cache, можно игнорировать
* . [ Cc ] боль
# но следите за каталогами, заканчивающимися на .cache
! ? * . [ Копия ] боль /

# Другие
ClientBin /
~ $ *
* ~
* .dbmdl
* .dbproj.schemaview
* .jfm
* .pfx
* .publishsettings
orleans.codegen.cs

# Включение файлов строгих имен может представлять угрозу безопасности
# (https://github.com/github/gitignore/pull/2483#issue-259490424)
# * .snk

# Поскольку существует несколько рабочих процессов, раскомментируйте следующую строку, чтобы игнорировать bower_components
# (https://github.com/github/gitignore/pull/1529#issuecomment-104372622)
# bower_components /

# RIA / Silverlight проекты
Generated_Code /

# Резервное копирование и отчет файлов из преобразования старого файла проекта
# до более новой версии Visual Studio. Резервные файлы не нужны,
# потому что у нас есть мерзавец ;-)
_UpgradeReport_Files /
Резервное копирование * /
UpgradeLog * .XML
UpgradeLog * .htm
ServiceFabricBackup /
* .rptproj.bak

# Файлы SQL Server
* .mdf
* .ldf
* .ndf

# Бизнес-аналитические проекты
* .rdl.data
* .bim.layout
* .bim_ * .settings
* .rptproj.rsuser
* - [ Bb ] ackup.rdl
* - [ Bb ] ackup  ( [ 0-9 ] ) .rdl
* - [ Bb ] ackup  ( [ 0-9 ] [ 0-9 ] ) .rdl

# Microsoft Fakes
FakesAssemblies /

# Файл настроек плагина GhostDoc
* .GhostDoc.xml

# Node.js Инструменты для Visual Studio
.ntvs_analysis.dat
node_modules /

# Visual Studio 6 сборка журнала
* .plg

# Файл опций рабочего пространства Visual Studio 6
* .opt

# Visual Studio 6 автоматически сгенерированный файл рабочей области (содержит какие файлы были открыты и т. Д.)
* .vbw

# Visual Studio LightSwitch построить вывод
** / * .HTMLClient / GeneratedArtifacts
** / * .DesktopClient / GeneratedArtifacts
** / * .DesktopClient / ModelManifest.xml
** / * .Server / GeneratedArtifacts
** / * .Server / ModelManifest.xml
_Pvt_Extensions

# Пакет менеджер зависимостей
.paket / paket.exe
Пакет-файлы /

# FAKE - F # Make
.fake /

# CodeRush личные настройки
.cr / personal

# Инструменты Python для Visual Studio (PTVS)
__pycache__ /
* .pyc

# Торт - раскомментируйте, если вы используете его
# инструменты / **
# ! tools / packages.config

# Tabs Studio
* .tss

# Конфигурационный файл Telerik's JustMock
* .jmconfig

# BizTalk построить вывод
* .btp.cs
* .btm.cs
* .odx.cs
* .xsd.cs

# Результаты анализа OpenCover UI
OpenCover /

# Вывод локального запуска Azure Stream Analytics
ASALocalRun /

# MSBuild Двоичный и структурированный журнал
* .binlog

# Файл конфигурации отладчика графического процессора NVidia Nsight
* .nvuser

# Рабочая папка MFractors (инструмент производительности Xamarin)
.mfractor /

# Локальная история для Visual Studio
.localhistory /

# BeatPulse временная база Healthcheck
healthchecksdb

# Резервная папка для инструмента преобразования ссылок на пакеты в Visual Studio 2017
MigrationBackup /

# Ionide (кросс-платформенная F # VS Code tools) рабочая папка
.ionide /